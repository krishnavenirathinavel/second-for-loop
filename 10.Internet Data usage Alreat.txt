 #include <stdio.h>

int main()
{
  int n;
  int i,total  = 0,data,highdata;
  scanf("%d",&n);
  for(i = 0; i < n; i++){
    scanf("%d",& data);
    total =  total + data;
    if(data > 2)
      
     {
       highdata = highdata + 1;
     }
  }
  printf("Total Data : %d\n",total);
  printf("High Usage Days : %d\n",highdata);
    return 0;
}